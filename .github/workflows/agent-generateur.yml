name: Agent Générateur Web

on:
  schedule:
    # Tous les samedis à 6h30 UTC = 7h30 heure française (hiver)
    - cron: '30 6 * * 6'
  workflow_dispatch:  # Permet de lancer manuellement pour tester

jobs:
  generate-site:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v3
      with:
        token: ${{ secrets.GITHUB_TOKEN }}
    
    - name: Setup Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'
    
    - name: Install dependencies
      run: |
        pip install anthropic google-api-python-client google-auth
    
    - name: Run Generator
      env:
        ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        GOOGLE_DRIVE_CREDENTIALS: ${{ secrets.GOOGLE_DRIVE_CREDENTIALS }}
        GOOGLE_DRIVE_FOLDER_ID: ${{ secrets.GOOGLE_DRIVE_FOLDER_ID }}
      run: python agents/agent_generateur_web.py
    
    - name: Commit and Push
      run: |
        git config --local user.email "action@github.com"
        git config --local user.name "GitHub Action"
        git add docs/index.html config/styles_preferences.json
        git diff --quiet && git diff --staged --quiet || git commit -m "🚀 Mise à jour automatique du site - $(date +'%Y-%m-%d')"
        git push
