# WORKFLOW DÉSACTIVÉ - Remplacé par veille-openai-v3.yml
# Pour réactiver : supprimer .disabled du nom de fichier

name: Veille OpenAI Complete (DÉSACTIVÉ)

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Workflow désactivé
        run: echo "⚠️ Ce workflow est désactivé. Utilisez veille-openai-v3.yml"
