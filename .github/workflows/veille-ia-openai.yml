name: Agents Veille IA - OpenAI (2 agents)

on:
  # Exécution quotidienne à 6h Paris (5h UTC en hiver, 4h UTC en été)
  schedule:
    - cron: '0 5 * * *'  # 6h Paris (hiver)
  
  # Déclenchement manuel
  workflow_dispatch:

jobs:
  agent-collecteur:
    name: "Agent 1 - Collecteur (GPT-4o-mini)"
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
      
      - name: Install dependencies
        run: |
          pip install --upgrade pip
          pip install -r requirements.txt
      
      - name: Run Agent 1 - Collecteur
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          TAVILY_API_KEY: ${{ secrets.TAVILY_API_KEY }}
        run: |
          echo "🚀 Lancement Agent 1 - Collecteur"
          cd agents
          python agent_collecteur_ia.py
      
      - name: Upload JSON filtré (artifact pour Agent 2)
        uses: actions/upload-artifact@v4
        with:
          name: articles-filtres-ia
          path: agents/articles_filtres_ia.json
          retention-days: 1

  agent-synthese:
    name: "Agent 2 - Synthèse (GPT-4o)"
    runs-on: ubuntu-latest
    needs: agent-collecteur  # Attend que Agent 1 soit terminé
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
      
      - name: Install dependencies
        run: |
          pip install --upgrade pip
          pip install -r requirements.txt
      
      - name: Download JSON filtré (depuis Agent 1)
        uses: actions/download-artifact@v4
        with:
          name: articles-filtres-ia
          path: agents/
      
      - name: Run Agent 2 - Synthèse
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          GOOGLE_DRIVE_CREDENTIALS: ${{ secrets.GOOGLE_DRIVE_CREDENTIALS }}
          GOOGLE_DRIVE_FOLDER_ID: ${{ secrets.GOOGLE_DRIVE_FOLDER_ID }}
        run: |
          echo "🚀 Lancement Agent 2 - Synthèse"
          cd agents
          python agent_synthese_ia.py
      
      - name: Summary
        if: success()
        run: |
          echo "✅ Pipeline 2-agents terminé avec succès"
          echo "📊 Agent 1 : Collecte + filtrage (GPT-4o-mini)"
          echo "📝 Agent 2 : Synthèse Markdown (GPT-4o)"
          echo "☁️  Fichier VeilleIA.md uploadé sur Google Drive"
