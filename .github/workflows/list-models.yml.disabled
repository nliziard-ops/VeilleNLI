# WORKFLOW DÉSACTIVÉ - Utilitaire de test
# Pour réactiver : supprimer .disabled du nom de fichier

name: List Models (DÉSACTIVÉ)

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Workflow désactivé
        run: echo "⚠️ Ce workflow est désactivé"
